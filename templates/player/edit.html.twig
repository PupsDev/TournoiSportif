{% extends 'base.html.twig' %}

{% block title %}Modifier le profil{% endblock %}

{% block body %}
    <div class="card-header" style="background-color:#303030;color:white;text-align:center;">
<h1>
    <div class="row">
    <div class="col-6">
    Modifier le profil
    </div>
    <div class="col" style="border-left:2px solid white; height:100%;">

                <a href="{{ path('player_show', {'id': player.id}) }}" class="btn btn-danger">Retour</a>
    </div>
    </h1>
</div>

    {{ include('player/_form.html.twig', {'button_label': 'Update'}) }}


{% endblock %}
