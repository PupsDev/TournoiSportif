<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="{{path('home')}}" id="esprit">Esprit Tournoi</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      
      <li class="nav-item">
        <a class="nav-link" href={{ path('turnaments_index_all') }}>Tournoi</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href={{ path('event_index') }}>Évènements</a>
      </li>
     
      
    </ul>
    <form class="form-inline my-2 my-lg-0">
      {% if app.user  %}
          {% if 'ROLE_ORGANIZER' in app.user.roles  %}
             <a href="{{ path('organizer_show', {'id': app.user.id}) }}">{{app.user.username}}</a>
          {% else %}
             <a href="{{ path('player_show', {'id': app.user.id}) }}">{{app.user.username}}</a>
            {% endif %}
     
      <a href="{{ path('app_logout') }}" class="btn btn-primary my-2 mx-sm-2" id="logout" type="button">Se déconnecter </a>

       {% else %}
         <a href="{{ path('app_register') }}" class="btn btn-primary my-2 mx-sm-2" id="login" type="button">S'enregistrer </a>
        <a href="{{ path('app_login') }}" class="btn btn-primary my-2 mx-sm-2" id="login" type="button">Se connecter </a>
      {% endif %}
      

      <input class="form-control mr-sm-2" type="search" id="search-query" placeholder="Rechercher" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" id="search-button" type="button">Go !</button>
    </form>
  </div>
</nav>